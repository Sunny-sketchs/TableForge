
pydantic~=2.11.9

# POSTGRESQL & DATABASE (For Metadata and Logs)
sqlalchemy~=2.0.43

# AGENT TOOLS


#Upload, Play, Loader, FileText, CheckCircle, XCircle from lucide-react
httpx

psycopg2~=2.9.11

#--- FastAPI & ASGI Server ---

fastapi>=0.109.0
uvicorn[standard]>=0.27.0

#--- Database & ORM ---

sqlalchemy>=2.0.25
psycopg2-binary>=2.9.9  # PostgreSQL driver
python-dotenv>=1.0.1   # For loading .env files locally

#--- LLM Integration ---

httpx>=0.27.0          # Used by the ChatService for calling the Gemini API

#--- Data Processing (The Heavy Lift) ---

pandas>=2.2.0
openpyxl>=3.1.2        # Recommended by Pandas for Excel export (useful utility)

#--- PDF Extraction (Camelot and its Dependencies) ---

camelot-py[cv]>=0.11.0 # Includes opencv-python and its dependencies
pdfminer.six>=20231221 # PDF parser used by Camelot

#--- Camelot requires the system library Ghostscript to be installed on the host OS.

#Note: For Docker deployment, Ghostscript must be installed via apt-get (as done in the Dockerfile).