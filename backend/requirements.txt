# ============================================
# TABLEFORGE BACKEND REQUIREMENTS
# ============================================

# --- FastAPI & ASGI Server ---
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6  # Required for file uploads

# --- Data Validation ---
pydantic>=2.11.9
pydantic-settings>=2.1.0  # For settings management

# --- Database & ORM ---
sqlalchemy>=2.0.43
psycopg2-binary>=2.9.9  # PostgreSQL driver (use this instead of psycopg2)
alembic>=1.12.1  # Database migrations (optional but recommended)

# --- Environment Variables ---
python-dotenv>=1.0.1

# --- LLM Integration (Google Gemini) ---
google-generativeai>=0.3.1  # Official Gemini SDK
httpx>=0.27.0  # HTTP client (also used by Gemini SDK)

# --- PDF Extraction (Camelot and Dependencies) ---
camelot-py[cv]>=0.11.0  # Includes opencv-python
pdfminer.six>=20231221  # PDF parser
PyPDF2>=3.0.1  # Additional PDF utilities
opencv-python-headless>=4.8.1.78  # Headless version for servers
ghostscript>=0.7  # Python wrapper for Ghostscript

# Note: Ghostscript system library must be installed via:
# apt-get install -y ghostscript python3-tk libsm6 libxext6 libxrender-dev

# --- Data Processing ---
pandas>=2.2.0
openpyxl>=3.1.2  # Excel support for pandas
numpy>=1.24.0  # Required by pandas

# --- Utilities ---
aiofiles>=23.2.1  # Async file operations
requests>=2.31.0  # HTTP requests
python-json-logger>=2.0.7  # Structured logging

# --- Development & Testing (Optional) ---
# pytest>=7.4.0
# pytest-asyncio>=0.21.0
# black>=23.0.0
# flake8>=6.0.0